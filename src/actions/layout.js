import { SHOW_EXPLORE, NEW_PALETTE } from "./types";

// Open discover

export const openDiscover = () => dispatch => {
  dispatch({ type: SHOW_EXPLORE });
};

// new pallete button

export const newPalette = () => dispatch => {};
